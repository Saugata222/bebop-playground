/**
 * Shared: Focus Ring
 *
 * Consistent focus indicator used across Button, Tag, Tab, Toolbar.
 * 2px black outer border + 1px white inner border.
 */

export const focusRing = {
  outerWidth: '2px',
  outerColor: '#000000',
  innerWidth: '1px',
  innerColor: '#ffffff',
} as const;
