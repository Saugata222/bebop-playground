{
  "name": "bebop-design-system",
  "version": "1.0.0",
  "description": "Bebop Design System — TypeScript design token library",
  "main": "index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npx tsx preview/src/shell.ts",
    "build:all": "for f in preview/src/*.ts; do case \"$f\" in *_inject*) ;; *) npx tsx \"$f\";; esac; done && npx tsx preview/src/_inject.ts",
    "serve": "python3 -m http.server 8765",
    "dev": "npm run build && npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@fluentui/tokens": "^1.0.0-alpha.23",
    "@fluentui/web-components": "^2.6.1"
  }
}
